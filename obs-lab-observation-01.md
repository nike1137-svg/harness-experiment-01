# 관찰 메모 01 — 하네스의 경계는 화면 어디에 드러나는가

- **일시**: 2026-09-02 10:45 ~ 11:06
- **목적**: 에이전트 루프 5단계(요청 → 상태 → 모델 → 도구 → 관측)가 실제 화면 어디에 드러나는지 관찰
- **용도**: 제출 과제 `experiment-01.md` 의 "4. 실행 기록" 섹션 재료

---

## 1. 관찰 조건 (fixture)

이 조건이 흔들리면 이후 비교가 무의미해지므로 그대로 고정해 기록한다.

| 항목 | 값 |
|---|---|
| 하네스 | Claude Code CLI |
| 실행 환경 | WSL Ubuntu (`/home/nike1`) |
| 모델 | `claude-sonnet-5` (화면 표시값 그대로) |
| 작업 폴더 | `~/obs-lab` |
| 초기 상태 | 빈 폴더 (`total 8`, `.` 와 `..` 뿐) |
| 입력 방식 | 자연어 요청만 사용. 셸 명령을 직접 입력하지 않음 |
| 적용 규칙 | 전역 `CLAUDE.md` (환경 표시 · 명령어 4원칙 · 단계별 승인) |

> 교재 기준 조건은 `Claude Code CLI 2.1.220`, 폴더에 `notes.txt` 한 건이 이미 있는 상태였다.
> 본 실습은 **빈 폴더에서 시작해 직접 생성**하는 순서로 진행했다. 조건 차이를 그대로 남긴다.

---

## 2. 관찰 카드

| 칸 | 관찰된 내용 |
|---|---|
| **조건** | Claude Code CLI (WSL), 모델 `claude-sonnet-5`, 작업 폴더 `~/obs-lab`, fixture = `notes.txt` 한 건 |
| **입력** | 자연어 요청 3건. 셸 명령을 직접 치지 않음 |
| **도구 호출** | `Bash(ls -la)` → `Bash(python3 -c ...)` → `Bash(cat ...)`. 공식 문서가 안내하는 `Glob`/`Read`가 아니라 **전부 Bash** |
| **결과** | `total 8`(빈 폴더) → `(No output)` → 파일 내용 2줄. **출력 없음도 관측으로 기록됨** |
| **승인** | 3건 모두 **승인 창 없이** 실행. 쓰기 작업에서도 창이 뜨지 않음 |
| **한계** | 이 화면만으로는 권한 규칙을 알 수 없다. 설정 파일을 따로 봐야 확인 가능 |

---

## 3. 실행 궤적 (Trajectory)

| # | 요청 (자연어) | 모델이 고른 도구 | 화면 출력 | 승인 창 |
|---|---|---|---|---|
| 1 | 이 폴더의 파일 목록을 보여 줘 | `Bash(ls -la /home/nike1/obs-lab)` | `total 8` / `.` `..` | 없음 |
| 2 | 응, 실행해줘 | `Bash(python3 -c "open(...,'w').write(...)")` | `(No output)` | **없음** |
| 3 | (모델 자발) | `Bash(cat /home/nike1/obs-lab/notes.txt)` | 파일 내용 2줄 | 없음 |

---

## 4. 사실 / 해석 분리

관찰 기록의 원칙: **눈에 보인 것**과 **내가 추론한 것**을 섞지 않는다.

| # | 사실 (화면에 보인 것) | 해석 (내가 추론한 것) |
|---|---|---|
| 1 | `● Bash(ls -la /home/nike1/obs-lab)` 표시 | 행동 포맷이 드러남 — 도구 이름 + 실행 명령어 전문까지 노출 |
| 2 | `Glob`/`Read`가 아니라 Bash 선택 | 공식 문서 권장 도구 ≠ 모델의 실제 선택 |
| 3 | `total 8`, `.` 와 `..` 만 존재 | 폴더가 실제로 비어 있음. `total`은 파일 크기 합이 아니라 디렉터리 엔트리 블록 수 |
| 4 | 쓰기 작업에도 승인 창 없음 | 하네스 강제 장치가 아니라 **모델이 규칙에 따라 말로 물어본 것**이었음 |
| 5 | `claude-sonnet-5` 모델명 표시 | 관찰 조건으로 반드시 기록해야 할 값 |
| 6 | `※ recap:` 내용이 턴마다 갱신됨 | 상태(State)가 누적이 아니라 **압축·재작성**되고 있음 |
| 7 | 시키지 않은 `cat` 검증 실행 | 결과만 보지 않고 관측으로 되돌아감 — 평가 루프의 축소판 |
| 8 | `cat` 결과 경로가 `/home/nike1/...` | 사전에 우려했던 `$USER` 치환이 정상 작동함을 검증 |

---

## 5. 핵심 발견

### ① "물어본 것"과 "권한 경계"는 다르다

화면에 나온 *"실행해도 될까요?"* 는 **모델의 예의**였다. 하네스가 띄운 승인 창이 아니었다.

| 구분 | 주체 | 우회 가능성 |
|---|---|---|
| 모델이 말로 묻기 | 모델 판단 (`CLAUDE.md` 규칙 때문) | 규칙이 없으면 그냥 실행됨 |
| 하네스 승인 창 | 하네스가 강제 | 뚫을 수 없음 |

읽기(`ls`)든 쓰기(`python3 ... 'w'`)든 **승인 창은 한 번도 뜨지 않았다.**
→ **화면에 보이지 않은 것도 관찰 결과다.** 권한 경계의 실체는 설정 문서에서 따로 확인해야 한다.

### ② 관측은 기본적으로 축약된다

처음 화면에는 `Listed 1 directory` 한 줄만 남았고, `Ctrl+O` 를 눌러야 `total 8 / drwxr-xr-x ...` 원본이 펼쳐졌다.
→ **화면엔 요약, 내부엔 원본.** 컨텍스트 관리가 눈에 보이는 지점.

### ③ 상태(State)는 쌓이지 않고 다시 쓰인다

```
(승인 대기 시)  ...I'm waiting on your go-ahead...
(작업 완료 후)  ...created notes.txt..., and verified its contents. Next step is deciding what to work on next.
```

같은 자리에서 내용이 통째로 교체됐다. 대화 누적이 아니라 **필요한 것만 남기는 압축**이다.

---

## 6. 루프 5단계 대응표

| 루프 단계 | 이번 실습에서 확인된 지점 |
|---|---|
| **요청(Request)** | 자연어 입력 3건이 화면 상단에 그대로 남음 |
| **상태(State)** | `※ recap:` 블록. 턴마다 갱신됨 |
| **모델(Model)** | `claude-sonnet-5` 표시. 도구 선택 판단을 수행 |
| **도구(Tool)** | `● Bash(...)` 줄. 명령어 전문까지 노출 |
| **관측(Observation)** | 출력 블록. 기본 축약, `Ctrl+O`로 원본 확인 |

---

## 7. 남은 질문 (다음 관찰 과제)

1. 승인 창은 **어떤 조건에서** 뜨는가 — 권한 설정 파일을 직접 확인해야 함
2. `Glob`/`Read` 대신 Bash를 고르는 기준은 무엇인가 — 요청 문구를 바꾸면 선택이 달라지는가
3. `recap` 압축이 **무엇을 버리는가** — 버려진 정보가 나중에 필요해지면 어떻게 되는가

---

## 8. 다음 단계

이 메모는 `experiment-01-handoff.md` 의 과제 중 **"4. 실행 기록"** 섹션에 그대로 투입한다.
같은 조건에서 `notes/` 기록 참조 유무를 비교하는 것이 다음 실험이다.
