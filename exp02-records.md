# 실험 02 기록표 (실측)

- 실험일: **2026-09-02**
- 실행 시각: **20:12 ~ 20:17**
- 환경: WSL Ubuntu 24.04 / Claude Code / **Sonnet 5** / **manual mode**
- 작업 폴더: `~/exp02/trial-1`
- 판정선: 3종 중 2종 이상에서 재지시 또는 시간 감소 → 지지 (**실험 01과 동일**)
- 게이트: 1차 재지시 총합 **2회 미만이면 중단** (실험 시작 전 확정)

---

## 조건표

| 항목 | 값 | 실험 01과 |
|---|---|---|
| 하네스 | Claude Code CLI | 동일 |
| Claude Code 버전 | **미기록** (실험 01은 v2.1.258) | 확인 불가 |
| 모델 | Sonnet 5 | 동일 |
| 권한 모드 | manual | 동일 |
| 실행 환경 | WSL Ubuntu 24.04 | 동일 |
| pytest | **7.4.4** | 동일 |
| 판정선·지표·절차 | 실험 01 그대로 | 동일 |
| **fixture 난이도** | **함정 4종 삽입** | **← 바뀐 것은 이것 하나** |

### fixture 무결성 확인 (실험 시작 전)

```
$ cd ~/exp02/fixtures && python3 -m pytest --version && python3 -m pytest test_me.py -q --tb=no
pytest 7.4.4
.FF.                                                    [100%]
FAILED test_me.py::test_median_even    - assert 3 == 2.5
FAILED test_me.py::test_percent_change - assert 20.0 == 25.0
2 failed, 2 passed in 0.03s
```

의도한 2건이 정확히 실패함을 확인한 뒤 시작했다. 검증 과정에서 생긴 캐시는 즉시 삭제했다.

---

## 1차 시도 — 기록 없음 (`~/exp02/trial-1/`)

| 과업 | 시작 | 종료 | 소요(분) | 처리(초) | 승인 | 재지시 | 통과 |
|---|---|---|---|---|---|---|---|
| ① parse-log | 20:12 | 20:13 | 1 | 50 | 2* | **0** | ✅ |
| ② refactor-util | 20:15 | 20:15 | **0** | 23 | 2* | **0** | ✅ |
| ③ fix-failing | 20:16 | 20:17 | 1 | 27 | 3 | **0** | ✅ |
| **합계** | | | **2** | **100** | **7** | **0** | **3/3** |

<sub>* ①②의 승인 횟수는 화면 기록에서 추정한 값이다. ③은 실측.</sub>

### 과업별 판정 근거 (`grading-key.md` 대조)

#### ① parse-log — 4개 기준 전부 충족

| 기준 | 결과 | 근거 (에이전트 출력) |
|---|---|---|
| ERROR 5건 | ✅ | "sample.log의 총 18개 로그 줄 중 ERROR 레벨 5건을 정확히 골라냈습니다" |
| 트레이스백이 독립 항목 아님 | ✅ | "8번 줄 ERROR 아래 트레이스백(9~12번 줄)은 레벨 표시가 없는 부가 정보라 별도 항목으로 세지 않음" |
| INFO 본문의 ERROR 제외 | ✅ | "14번 줄은 메시지에 'ERROR'라는 단어만 있고 실제 레벨은 INFO라서 정상적으로 제외됨" |
| 다른 시각 포맷 포함 | ✅ | "15번 줄은 타임스탬프 형식이 달랐지만 정규식에 두 형식 모두 넣어서 정상 인식됨" |

#### ② refactor-util — 4개 기준 전부 충족

| 기준 | 결과 |
|---|---|
| 3개 전부 타입 힌트 | ✅ `parse_duration`, `chunk_list`, `safe_get` |
| 3개 전부 독스트링 | ✅ Args / Returns 형식 |
| 중첩 리스트 표현 | ✅ `List[List[Any]]` |
| 로직 미변경 | ✅ diff에서 함수 본문에 `+`/`-` 없음 |

#### ③ fix-failing — 2개 기준 전부 충족

| 기준 | 결과 |
|---|---|
| 4 passed | ✅ |
| 테스트부 원문 유지 | ✅ diff에 구현부만 (median 2줄 추가, percent_change 1줄 교체) |

---

## 게이트 판정

```
재지시 총합 0회  <  기준 2회
```

> ### 판정: **중단** — 2차 조건(기록 참조) 미실시

사전에 정한 게이트를 그대로 적용했다. **결과를 본 뒤 게이트를 무르지 않았다.**

---

## notes/reflection.md

**미작성.** 1차에서 실패가 한 건도 발생하지 않아 **도출할 반성이 없었다.**
외부에서 정답을 넣어 만들면 실험 01이 지적한 "정답 요약" 문제를 그대로 반복하게 되므로,
작성하지 않고 중단을 택했다.

---

## 2차 시도 — 기록 참조 (`~/exp02/trial-2/`)

**미실시** (게이트 중단)

---

## 판정선 대입

**미적용.** 2차 조건이 없으므로 비교가 성립하지 않는다.
실험 02는 **판정 이전 단계(게이트)에서 종료**됐다.

---

## 이 기록에서 나온 것

| # | 관찰 | 의미 |
|---|---|---|
| 1 | 함정 4종을 심었으나 재지시 0회 | 실험 01의 "난이도가 원인" 추정이 **반증**됨 |
| 2 | 과업 ②가 1차에서 이미 0분 | 시간 지표도 바닥. 분 단위 해상도의 한계 |
| 3 | 처리 시간 합계 100초 | 초 단위 지표는 아직 여유가 있음 → 실험 03 후보 |
| 4 | 승인 7회 (실험 01은 6회) | manual mode가 양 실험 모두에서 정상 작동 |

---

## 미기록 항목 (한계로 남김)

| 항목 | 사유 |
|---|---|
| Claude Code 버전 | 실행 시 확인하지 않았다. 실험 01(v2.1.258)과 동일한지 보증할 수 없다 |
| ①② 승인 횟수 | 화면 추정치. ③만 실측 |
| parse.py 출력 표 원문 | 화면 밖으로 스크롤됨. 판정은 에이전트의 요약(줄 번호까지 명시)에 근거했다 |
