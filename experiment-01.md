# 실험 01 — 성공 기록 참조가 다음 실행을 개선하는가

- **실험일**: 2026-09-02
- **대상 기능 축**: 경험·스킬 축적 / 반성(Reflexion)
- **판정 결과**: **가설 기각**

---

## 1. 배경

### 출발점이 된 불편

전날(2026-09-01) n8n + Colab vLLM 연동 작업에서 다음 네 가지가 반복됐다.

| 겪은 일 | 문제의 성격 |
|---|---|
| "정지해도 서버는 영향 없다"는 안내를 받고 그대로 했더니 서버가 죽었다 | 틀린 설명이 그대로 지시가 됐다 |
| 강사 원본에 있던 옵션 3개가 받은 코드에서 빠져 있었다 | 한 번 더 묻지 않으면 드러나지 않았다 |
| 응답이 느릴 때 "기다리라"는 답만 반복됐다 | 판정 근거 없는 답이 반복됐다 |
| 12분이 정상 소요 시간인데 멈춘 것으로 판정했다 | 판정 기준을 기록해두지 않았다 |

공통점은 **한 번 겪은 실패가 다음 시도에 반영되지 않았다**는 것이다. 같은 종류의 확인을 매번 처음부터 다시 했다.

### 참조한 논문 가설

- **Reflexion** (Shinn et al., NeurIPS 2023, arXiv:2303.11366)
  - `trial → error → reflection → memory` 루프를 자연어 피드백만으로 구성
  - **가중치를 바꾸지 않는다** — 재학습 없이 언어 기록만 사용
  - 보고 수치: HumanEval에서 GPT-4 기준 80.1% → 91%
  - 기억은 무한 축적하지 않고 **최근 1~3개만** 남겨 다시 투입
- 관련 연구: ExpeL (AAAI 2024), Voyager, AWM (arXiv:2409.07429, 프리프린트) — 성공 경험을 재사용 가능한 형태로 저장하면 다음 과업이 개선된다는 공통 주장

### 근거 수준 표기

| 근거 | 등급 |
|---|---|
| Reflexion (NeurIPS 2023) | 학회 논문 — 높음 |
| 전날 작업 기록 | 본인 1차 기록 — 조건 확인 가능 |

**원 조건과 본 실험의 차이**: Reflexion이 다룬 과업은 여러 번 실패하는 난이도의 코딩·의사결정 벤치마크였다. 본 실험의 과업은 그보다 훨씬 단순하다. 이 차이가 결과 해석에 결정적으로 작용했다(5절 참조).

---

## 2. 가설

| 칸 | 내용 |
|---|---|
| **주장** | 실패를 검증 가능한 문장으로 남겨 다음 시도에 참조시키면, 같은 작업에서 재지시 횟수와 소요 시간이 줄어든다 |
| **근거** | Reflexion (NeurIPS 2023, arXiv:2303.11366). 가중치 변경 없이 자연어 피드백만으로 HumanEval 80.1% → 91% |
| **검증 방법** | 동일 과업 3종을 ① 기록 없이 ② 반성 기록 참조하며 각 1회 실행하고, 재지시 횟수·소요 시간·승인 횟수를 비교 |
| **사전 판정선** | 3종 중 **2종 이상**에서 재지시 횟수 또는 소요 시간이 감소하면 **지지**, 그 외 **기각** |
| **한계** | 각 조건 1회 시행이므로 우연을 배제할 수 없다. 반성 문장의 품질을 통제하지 못한다 |

판정선은 **실험 시작 전에** 확정했고, 결과 확인 후 변경하지 않았다.

---

## 3. 실험 설계

### 고정 (fixture)

| 항목 | 값 |
|---|---|
| 하네스 | Claude Code CLI **v2.1.258** |
| 모델 | **Sonnet 5** |
| 권한 모드 | **manual mode** (양 조건 동일) |
| 실행 환경 | WSL Ubuntu 24.04 (`/home/nike1`) |
| 테스트 러너 | pytest **7.4.4** (apt 설치) |
| 입력 파일 | `sample.log`, `util.py`, `calc.py`, `test_me.py` — `fixture/` 원본에서 매 시도 복사, 원본 미수정 |
| 프롬프트 | 3종 모두 문장 고정. 2차에만 앞에 `reflection.md 를 먼저 읽고,` 를 추가 |

### 조작 (단일 변수)

| 조건 | 폴더 | 반성 기록 |
|---|---|---|
| 1차 | `trial-1` | 없음 |
| 2차 | `trial-2` | `reflection.md` 배치 + 프롬프트에서 참조 지시 |

### 측정

| 지표 | 정의 |
|---|---|
| 소요 시간 | 프롬프트 입력 ~ 완료 판정 (분) |
| 승인 횟수 | 권한 승인 창에서 Yes를 누른 횟수 |
| 재지시 횟수 | 방향을 바꾸거나 다시 시킨 입력 횟수 (승인 제외) |

> **승인 횟수 정의의 근거** — 사전 관찰 기록 `obs-lab-observation-01.md` 5절에서,
> 화면에 뜨는 *"실행해도 될까요?"* 에 두 종류가 있음을 확인했다.
>
> | 구분 | 주체 | 우회 가능성 |
> |---|---|---|
> | 모델이 **말로** 묻기 | 모델 판단 (전역 `CLAUDE.md` 규칙 때문) | 규칙이 없으면 그냥 실행됨 |
> | 하네스 **승인 창** | 하네스가 강제 | 뚫을 수 없음 |
>
> 본 실험에서 세는 것은 **후자(하네스 승인 창)** 뿐이다. 전자는 모델의 발화이므로
> 권한 경계가 아니다. 이 구분이 없으면 `CLAUDE.md` 규칙 변경만으로 지표가 움직인다.
>
> 같은 관찰에서, auto mode일 때는 쓰기 작업에서도 승인 창이 **한 번도 뜨지 않았다.**
> 실험 01 2차 시행에서 발생한 auto mode 오염 사고(4절)가 왜 측정값을 바꾸는지가
> 여기서 설명된다.

### 과업과 완료 판정

| 작업 | 과업 | 완료 판정 |
|---|---|---|
| parse-log | `sample.log` 에서 ERROR 줄만 표로 출력하는 `parse.py` 작성 | ERROR **3건** 출력 |
| refactor-util | `util.py` 함수 3개에 타입 힌트·독스트링 추가 | 3개 모두 |
| fix-failing | `test_me.py` 가 통과하도록 `calc.py` 수정 (`test_me.py` 수정 금지) | **5 passed** |

---

## 4. 실행 기록

### 반성 기록 원문 (`notes/reflection.md`)

```markdown
# 1차 시도 반성 기록

## parse-log
- 로그 형식은 `YYYY-MM-DD HH:MM:SS LEVEL 메시지` 이며, ERROR는 3건이다.
- 다음 시도에서는 파일 형식 확인 없이 바로 이 정규식 형태로 작성한다.

## refactor-util
- util.py 함수는 slugify, chunk, merge_defaults 3개다.
- chunk는 List[List[Any]], merge_defaults는 Dict[Any, Any] 반환이다.
- 다음 시도에서는 파일을 읽은 뒤 3개를 한 번에 수정한다.

## fix-failing
- 실패 원인은 divide(1, 0)과 average([]) 두 곳의 예외다.
- divide는 b == 0일 때 None, average는 빈 리스트일 때 0을 반환해야 한다.
- test_me.py는 수정 대상이 아니다. calc.py만 고친다.
```

문장은 6절 원칙(검증 가능성)에 따라 "잘 하자"가 아니라 **"다음 시도에서 무엇을 한다"** 형태로 작성했다.

### 1차 시도 — 기록 없음

| 작업 | 시작 | 종료 | 소요 | 승인 | 재지시 | 판정 |
|---|---|---|---|---|---|---|
| ① parse-log | 18:25 | 18:28 | 3분 | 2 | 0 | ✅ ERROR 3줄 |
| ② refactor-util | 18:28 | 18:29 | 1분 | 2 | 0 | ✅ 함수 3개 |
| ③ fix-failing | 18:29 | 18:30 | 1분 | 2 | 0 | ✅ 5 passed |
| **합계** | | | **5분** | **6** | **0** | |

### 2차 시도 — 기록 참조

| 작업 | 시작 | 종료 | 소요 | 승인 | 재지시 | 판정 |
|---|---|---|---|---|---|---|
| ① parse-log | 18:35 | 18:37 | 2분 | 2 | 0 | ✅ ERROR 3줄 |
| ② refactor-util | 18:37 | 18:38 | 1분 | 2 | 0 | ✅ 함수 3개 |
| ③ fix-failing | 18:38 | 18:39 | 1분 | 2 | 0 | ✅ 5 passed |
| **합계** | | | **4분** | **6** | **0** | |

### 실행 중 발생한 조건 오염 1건 (기록)

2차 시도 첫 시행에서 권한 모드가 **auto mode** 로 전환된 상태로 시작됐다. 자동 승인이 걸리면 승인 횟수가 조작 변수와 무관하게 줄어든다.

조치: 시행을 중단하고 `trial-2` 폴더를 삭제한 뒤 fixture에서 재생성하여 `manual mode` 로 다시 시작했다. 오염된 시행의 데이터는 폐기했다.

이 사고 자체가 **권한 모드가 측정값을 바꾼다**는 것을 실증했다.

---

## 5. 결과와 판정

### 판정선 대입

| 작업 | 재지시 감소 | 시간 감소 | 판정선 충족 |
|---|---|---|---|
| ① parse-log | 0 → 0 (변화 없음) | 3분 → 2분 | ✅ |
| ② refactor-util | 0 → 0 | 1분 → 1분 | ❌ |
| ③ fix-failing | 0 → 0 | 1분 → 1분 | ❌ |

충족 **1종**. 사전 판정선은 **2종 이상** 이었다.

> ### 판정: 가설 기각

### 무엇이 판정을 갈랐는가

**재지시가 양 조건 모두 0회였다.** 0에서 더 줄어들 수 없으므로 이 지표는 애초에 차이를 관찰할 수 없었다 — **바닥 효과(floor effect)** 다.

원인은 과업 난이도다. 3종 모두 1차 시도에서 재지시 없이 완료됐다. Reflexion이 다룬 과업은 여러 번 실패하는 난이도였는데, 본 실험 fixture는 실패 자체가 발생하지 않았다. **반성이 개입할 여지가 없는 조건에서 반성 효과를 측정하려 한 설계 오류**다.

### 사후에 발견한 보조 지표 (판정에 사용하지 않음)

화면에 모델 처리 시간이 초 단위로 기록돼 있었다.

| 작업 | 1차 | 2차 | 변화 |
|---|---|---|---|
| ① parse-log | 32s | 18s | -44% |
| ② refactor-util | 16s | 15s | -6% |
| ③ fix-failing | 11s | 10s | -9% |
| 합계 | 59s | 43s | -27% |

세 작업 모두 감소했다. 그러나 이 지표는 **결과를 본 뒤에 인지한 것**이므로 판정에 사용하지 않았다. 사후에 유리한 지표로 판정선을 갈아끼우면 실험이 무효가 된다.

다음 실험의 **지표 후보**로만 남긴다.

### 관찰된 행동 차이 (정성)

1차 ①번은 `sample.log` 형식을 먼저 확인한 뒤 `parse.py` 를 작성했다. 2차 ①번은 `reflection.md` 와 `sample.log` 를 함께 읽고 곧바로 작성했다. 형식 탐색 단계가 생략됐고, 이것이 ①번의 시간 감소와 대응한다.

②③번은 1차에서도 파일을 한 번 읽고 바로 수정했으므로 생략할 단계가 없었다.

---

## 6. 후속 실험 02 — 난이도 가설을 실제로 시험했다

실험 01의 결론은 **"재지시가 0인 것은 과업 난이도가 낮아서다"** 였다.
이것은 관찰이 아니라 **추정**이었다. 같은 날 후속 실험으로 이 추정을 직접 시험했다.

### 6.1 설계 — 조작 변수를 다시 하나로

실험 01과 **판정선·지표·절차·환경을 전부 동일하게** 두고, **fixture 난이도만** 바꿨다.

| 항목 | 실험 01 | 실험 02 |
|---|---|---|
| 환경 | WSL Ubuntu 24.04 | 동일 |
| 모델 | Sonnet 5 | 동일 |
| 권한 모드 | manual | 동일 |
| pytest | 7.4.4 | 동일 |
| 판정선 | 3종 중 2종 이상 감소 | 동일 |
| 측정 | 시간 / 승인 / 재지시 | 동일 |
| **fixture 난이도** | 평이 | **함정 3종 삽입** |

작업 폴더: `~/exp02/fixtures` (원본) / `~/exp02/trial-1` (1차 작업장)

### 6.2 재지시를 발생시키기 위한 장치

재지시는 결과가 기준과 어긋나야 발생한다. 그래서 두 가지를 도입했다.

1. **완료 판정 기준을 프롬프트에 적지 않는다.** 진행자만 `grading-key.md` 로 보유한다.
2. **재지시 문구에 정답을 넣지 않는다.** "무엇이 틀렸다"까지만 말한다.

fixture에 심은 함정:

| # | 함정 | 노린 오답 |
|---|---|---|
| 1 | ERROR 다음 줄에 들여쓴 스택 트레이스 4줄 | 트레이스백을 별개 ERROR로 세는 것 |
| 2 | `INFO ... user reported an ERROR in the UI form` | 본문의 단어 "ERROR"에 걸려 오탐하는 것 |
| 3 | `28/Aug/2026:09:18:22` — 한 줄만 다른 시각 포맷 | 정규식에서 누락하는 것 |
| 4 | `median` 짝수 처리 누락 / `percent_change` 분모 오류 | 한 쪽만 고치는 것 |

### 6.3 사전 게이트 (실험 시작 전 확정)

> 1차 3과업을 끝낸 시점에 **재지시 총합이 2회 미만**이면 2차 조건으로 넘어가지 않고 중단한다.

난이도를 올려도 재지시가 안 나오면, 2차를 돌려도 판정이 불가능하기 때문이다.

### 6.4 결과 — 1차 시도 (`trial-1`, 기록 없음)

| 과업 | 시작 | 종료 | 소요 | 처리(초) | 승인 | 재지시 | 통과 |
|---|---|---|---|---|---|---|---|
| ① parse-log | 20:12 | 20:13 | 1분 | 50s | 2* | **0** | ✅ |
| ② refactor-util | 20:15 | 20:15 | 0분 | 23s | 2* | **0** | ✅ |
| ③ fix-failing | 20:16 | 20:17 | 1분 | 27s | 3 | **0** | ✅ |
| 합계 | | | 2분 | 100s | 7 | **0** | 3/3 |

<sub>* ①②의 승인 횟수는 화면 기록에서 추정한 값이다.</sub>

### 6.5 게이트 판정 — 중단

```
재지시 총합 0회  <  기준 2회  ->  2차 조건 미실시
```

사전에 정한 게이트를 그대로 적용해 여기서 중단했다.
결과를 본 뒤 게이트를 무르지 않았다.

### 6.6 발견 — 실험 01의 추정은 반증됐다

| | 주장 | 검증 |
|---|---|---|
| 실험 01 | "재지시 0의 원인은 과업 난이도" | 추정 |
| **실험 02** | 난이도를 올림 (함정 4종) | **재지시 여전히 0 → 추정 반증** |

에이전트가 함정을 처리한 방식:

| 함정 | 처리 |
|---|---|
| 스택 트레이스 | "레벨 표시가 없는 부가 정보라 별도 항목으로 세지 않음" — 스스로 판단해 제외 |
| INFO 본문의 ERROR | "메시지에 단어만 있고 실제 레벨은 INFO라 정상적으로 제외됨" |
| 다른 시각 포맷 | 정규식에 두 형식을 모두 넣어 처리 |
| 버그 2개 | 코드를 읽는 단계에서 원인 두 개를 정확히 지목한 뒤 수정 |

즉 **난이도를 올린 것이 아니라, 모델이 그 난이도를 그대로 넘겼다.**

### 6.7 그래서 진짜 원인은 지표에 있다

재지시는 **모델이 틀려야** 발생하는 지표다. 틀리지 않으면 값은 영원히 0이다.

> **이 조합(Claude Code + Sonnet 5 + 단일 파일 수십 줄 과업)에서는
> "재지시 횟수"라는 지표 자체가 작동하지 않는다.**

난이도를 더 올려도 같은 결과가 반복될 가능성이 높다. 바꿔야 할 것은 난이도가 아니라 **지표** 또는 **과업의 규모**다.

소요 시간도 같은 문제를 겪었다. 과업 ②는 1차에서 이미 **0분**이라, 2차에서 줄어들 여지가 물리적으로 없었다.

---

## 7. 한계와 다시 검토할 조건

### 7.1 이 실험들로 확인할 수 없었던 것

| # | 한계 | 내용 |
|---|---|---|
| 1 | **시행 횟수** | 각 조건 1회. 우연을 배제할 수 없다 |
| 2 | **시간 해상도** | 분 단위 측정이라 1분 미만 차이가 관측되지 않는다. 실험 02 과업 ②는 1차에서 이미 0분이었다 |
| 3 | **바닥 효과** | 재지시 0회 조건에서는 감소를 관찰할 수 없다. 실험 02에서 난이도를 올려도 해소되지 않았다 |
| 4 | **반성 문장의 성격** | 실험 01의 `reflection.md` 는 실패에서 도출된 반성이 아니라 정답 요약에 가까웠다. 따라서 Reflexion이 아니라 **사전 정보 제공 효과**를 본 것이다 |
| 5 | **지표 불일치** | Reflexion·ExpeL 등은 **성공률**로 보고했으나 본 실험은 **재지시·시간**으로 쟀다. 같은 것을 잰 것이 아니다 |
| 6 | **진행자 오염** | 진행자가 가설·판정선·정답을 알고 있는 상태에서 재지시를 입력했다. 실험 02는 재지시 문구를 사전에 문서로 고정해 완화했으나 제거하지는 못했다 |
| 7 | **하네스 1종** | Claude Code / Sonnet 5 / manual mode **조합 하나**만 관찰했다. 행동 포맷·컨텍스트 관리·권한 모델이 다른 하네스에서는 결과가 달라질 수 있다 |
| 8 | **근거 표기 정정** | 본문 1절의 "AWM (ACL 2024)" 는 오기였다. 원문 확인 결과 **arXiv:2409.07429 프리프린트**이며 초록 페이지에 학회 표기가 없다. 아래 근거 등급표로 대체한다 |

### 7.2 근거 등급 (원문 확인 후 정정)

| 논문 | 출처 | 등급 | 저장하는 것의 형태 | 보고 수치 |
|---|---|---|---|---|
| Reflexion | Shinn et al., **NeurIPS 2023**, arXiv:2303.11366 | 동료심사 학회 | 자연어 반성 (최근 1~3개) | HumanEval 80.1% -> 91% |
| ExpeL | Zhao et al., **AAAI 2024**, arXiv:2308.10144 | 동료심사 학회 | 자연어 insight + 과거 궤적 | HotpotQA 36% / ALFWorld 50~55% / FEVER 63% -> 70% |
| Voyager | arXiv:2305.16291 | **프리프린트** | 실행 가능 코드 스킬 | 아이템 3.3배 / 기술트리 최대 15.3배 |
| AWM | Wang et al., arXiv:2409.07429 | **프리프린트** | 워크플로 (반복 루틴) | Mind2Web +24.6% / WebArena +51.1% |
| WikiSkill | Google Research, arXiv:2608.27454 | **프리프린트** | 영속 위키 + 스킬 (2층) | 49.5% -> 68.1% (Gemini-1.5-Flash) |

> 다섯 편 모두 **성공률**로 보고했다. **재지시 횟수를 지표로 쓴 논문은 없다.**
> 본 실험은 논문 결과를 재현한 것이 아니라, 다른 지표로 옮겨 시험한 것이다 (한계 5).

### 7.3 다음에 바꿀 조건

실험 02가 "난이도 상향" 안을 반증했으므로, 다음 조건은 **지표와 규모** 쪽으로 옮긴다.

#### [실험 03] 지표와 과업 규모 — 하네스는 1종 고정

- **지표 교체**: 재지시 횟수 대신 아래 후보로 바꾼다
  - 모델 처리 시간(초) — 실험 01에서 100초대 감소가 관측됐던 지표
  - 도구 호출 횟수 (Read / Bash / Edit 각각)
  - 탐색 단계 생략 여부 (파일 형식 확인을 건너뛰는가)
- **과업 규모 확대**: 단일 파일 수십 줄이 아니라 **여러 파일에 걸친 과업**으로. 모델이 한 번에 맞히지 못하는 규모여야 재지시가 존재할 수 있다
- **반복**: 각 조건 3회 이상, 초 단위 기록
- **판정선은 실험 전 확정**
- **선행 조건**: 1차에서 재지시가 여전히 0이면, 재지시 지표를 완전히 폐기하고 처리 시간으로 전환한다

#### [실험 04] 일반화 — 하네스 2종 교차

- 대상: **Claude Code (Sonnet 5)** / **OpenClaw (Gemini)** — 3과업 x 2조건 x 3반복 x 2하네스 = 36시행
- 각 하네스 **내부에서만** 기록 참조 유무를 비교한다. **하네스 간 절대값은 비교하지 않는다**
- 판정: 두 하네스 모두에서 감소가 재현되면 하네스·모델과 무관한 효과. 한 쪽만이면 원인 분리 불가로 기록
- **한계**: 하네스와 모델이 동시에 다르므로, 차이가 나도 하네스 때문인지 모델 때문인지 구분할 수 없다. 모델을 고정하려면 별도 유료 API가 필요해 이번 범위에서 제외했다
- **한계**: 표본 2종이라 "일반화"까지는 주장할 수 없다. "적어도 다른 조합 하나에서도 재현/불재현됐다"까지가 한계
- 재지시·승인의 정의는 하네스 중립적으로 재작성한다 (OpenClaw는 승인 흐름이 다르다)

#### 순서를 지키는 이유

```
타당도(실험 03)  ->  일반화(실험 04)
```

바닥 효과가 남아 있는 상태로 하네스를 늘리면, 고장난 자를 두 개로 늘리는 것과 같다.
실험 03에서 지표가 실제로 작동하는 것이 확인된 뒤에 실험 04로 넘어간다.

---

## 8. 제출 루브릭 자가 점검

| 항목 | 자평 | 근거 |
|---|---|---|
| **가설의 근거를 명확히 짚었나** | ✅ | Reflexion arXiv:2303.11366 명시. 원문 확인 후 5편의 **근거 등급표**를 7.2절에 작성하고, 초기 "AWM (ACL 2024)" 오기를 정정했다. 논문 지표(성공률)와 본 실험 지표(재지시)의 불일치도 명시했다 |
| **공정한 비교를 설계했나** | ✅ | fixture 원본 미수정, 프롬프트 문장 고정, 조작 변수 1개. 실험 02에서는 완료 기준을 프롬프트에서 분리하고 재지시 문구까지 사전 문서화했다. auto mode 오염 발생 시 시행을 폐기하고 재실행했다 |
| **나의 불편을 가설로 바꿨나** | ✅ | 전날 작업의 실패 4건에서 출발, 반증 가능한 형태로 서술, 실제로 **기각**됐다. 나아가 기각 원인에 대한 추정(난이도)까지 실험 02로 다시 시험해 **반증**했다 |

### 실험 파일 목록 (재현용)

| 파일 | 내용 |
|---|---|
| `grading-key.md` | 실험 02 진행자 전용 완료 기준 + 재지시 문구 (실험 시작 전 확정, v2) |
| `prompts.md` | 실험 02 프롬프트 원문 (완료 조건 미포함) |
| `setup_exp02.py` | 실험 02 작업 폴더·fixture 생성 스크립트 |
| `exp02-records.md` | 실험 02 기록표 (실측값·판정 근거·미기록 항목) |
| `fixtures/` | 함정 4종을 심은 `sample.log`, `util.py`, `test_me.py` |
| `obs-lab-observation-01.md` | **사전 관찰 기록.** 에이전트 루프 5단계가 화면 어디에 드러나는지, 그리고 "모델이 말로 묻는 것"과 "하네스 승인 창"이 어떻게 다른지를 규명했다. 3절 `승인 횟수` 지표 정의의 근거 |
