# 진행자 전용 — 완료 판정 기준 (grading key) v2

> **이 파일은 피험자 세션에 절대 보이면 안 된다.**
> `trial-1/`, `trial-2/` 안에 두지 않는다.
>
> 확정: 2026-09-02, **실험 시작 전**. 이후 수정하지 않는다.

---

## 왜 기준을 프롬프트에 적지 않는가

프롬프트에 완료 조건을 전부 적으면 한 번에 맞춘다. 그러면 재지시가 발생하지
않고, 실험 01과 똑같은 **바닥 효과**가 재현된다.

기준을 진행자만 보유하면, 결과가 기준과 어긋날 때 재지시가 발생한다.
**이 기준은 1차·2차 조건에 완전히 동일하게 적용한다.**

---

## 공통 규칙

### 재지시 상한 (v2 추가)

> **한 과업당 재지시 5회를 넘으면 그 과업을 "미완료"로 기록하고 다음 과업으로 넘어간다.**

양 조건에 동일하게 적용한다. 미완료도 데이터다. 한 과업에 시간이 무한정
쏠리는 것을 막는다.

### 재지시 문구 원칙

> **"무엇이 틀렸다"까지만 말하고, "무엇이 정답이다"는 말하지 않는다.**

정답을 주면 반성 실험이 아니라 정답 주입 실험이 된다.
(실험 01이 6장에서 스스로 지적한 함정이다.)

### 세는 규칙 (실험 01과 동일)

| 센다 (재지시) | 세지 않는다 |
|---|---|
| 아래 재지시 문구를 보낸 횟수 | 권한 승인 창의 Yes (승인 횟수로 따로 셈) |
| 방향을 바꾸는 추가 입력 | "진행", "응" 같은 단순 승인 답변 |

---

## 과업 1 — parse-log

프롬프트: `sample.log 에서 오류(ERROR)만 골라 표로 정리하는 parse.py 를 작성해라. 작성 후 실행해서 결과를 보여줘라.`

### 통과 기준 (4개 전부 충족)

| # | 기준 | 확인 방법 |
|---|---|---|
| 1 | ERROR 항목이 **5건** | 출력 행 수 |
| 2 | **트레이스백 4줄이 독립된 ERROR 항목으로 출력되지 않음** | 출력에 `Traceback`, `File "worker.py"`, `ValueError` 가 독립 행으로 없을 것 |
| 3 | `09:17:00 INFO ... user reported an ERROR in the UI form` 이 **제외**됨 | 출력에 `UI form` 문구 없을 것 |
| 4 | `28/Aug/2026:09:18:22 ERROR [auth]` 가 **포함**됨 | 출력에 `auth` 행 있을 것 |

> 기준 2는 v2에서 부정형 하나로 못박았다. "포함 또는 병합" 같은
> 해석의 여지를 없애기 위해서다.

### ERROR 5건 정답 (진행자 확인용)

```
1. 2026-08-28 09:14:02  db      connection refused host=10.0.0.7 port=5432
2. 2026-08-28 09:14:02  db      retry 1/3 failed
3. 2026-08-28 09:16:10  worker  task 4417 crashed
4. 28/Aug/2026:09:18:22 auth    token signature mismatch user=ktm
5. 2026-08-28 09:20:01  db      connection refused host=10.0.0.7 port=5432
```

### 재지시 문구 (해당 기준 위반 시 이것만 그대로 사용)

| 위반 | 보낼 문구 |
|---|---|
| 기준 1 | `출력된 오류 건수가 맞지 않다. 다시 확인해라.` |
| 기준 2 | `스택 트레이스가 별개 항목으로 잡혔다. 수정해라.` |
| 기준 3 | `오류가 아닌 줄이 섞여 있다. 다시 확인해라.` |
| 기준 4 | `누락된 오류 줄이 있다. 다시 확인해라.` |

---

## 과업 2 — refactor-util

프롬프트: `util.py 의 함수 3개 전부에 타입 힌트와 독스트링을 추가해라. 함수의 동작은 바꾸지 마라.`

### 통과 기준 (v2에서 기준 3 완화)

| # | 기준 |
|---|---|
| 1 | `parse_duration`, `chunk_list`, `safe_get` **3개 전부**에 타입 힌트 |
| 2 | 3개 전부에 독스트링 |
| 3 | `chunk_list` 반환형에 **중첩 리스트임이 드러나면 통과** (`list[list[str]]`, `List[List[Any]]`, `list[list]` 모두 인정. 표기 스타일은 묻지 않는다) |
| 4 | 함수 본문 로직이 바뀌지 않음 (diff 확인) |

> 기준 3을 완화한 이유: 표기 스타일을 엄격히 잡으면 실제로 틀린 것이
> 아닌데 재지시가 올라간다. **가짜 재지시는 측정값을 오염시킨다.**

### 재지시 문구

| 위반 | 보낼 문구 |
|---|---|
| 기준 1 | `타입 힌트가 빠진 함수가 있다.` |
| 기준 2 | `독스트링이 빠진 함수가 있다.` |
| 기준 3 | `반환 타입 표기가 실제 반환값과 맞지 않는다.` |
| 기준 4 | `동작을 바꾸지 말라고 했는데 로직이 변경됐다.` |

---

## 과업 3 — fix-failing

프롬프트: `test_me.py 를 pytest 로 실행하면 2건이 실패한다. 테스트부는 수정하지 말고, 구현부만 고쳐서 4건 전부 통과시켜라.`

### 통과 기준

| # | 기준 |
|---|---|
| 1 | `pytest` 결과 **4 passed** |
| 2 | 테스트 함수 4개가 **원문 그대로** (diff 확인) |

### 정답 (진행자 확인용)

- `median`: 길이가 짝수면 가운데 두 값의 평균을 반환해야 한다
- `percent_change`: 분모가 `new` 가 아니라 `old` 여야 한다

### 재지시 문구

| 위반 | 보낼 문구 |
|---|---|
| 기준 1 | `아직 통과하지 않는 테스트가 있다.` |
| 기준 2 | `테스트부를 수정하지 말라고 했다. 되돌려라.` |

---

## 사전 판정선 (실험 01과 동일 — 변경 금지)

> 3종 중 **2종 이상**에서 **재지시 횟수 또는 소요 시간이 감소**하면 가설 지지.
> 그 외에는 기각.

보조 기록: 재지시 단독 기준의 결과도 함께 적되, **판정에는 사용하지 않는다.**
사후에 유리한 지표로 갈아끼우는 것을 막기 위해 지금 선언한다.

---

## 게이트 조건

1차 조건 3과업을 끝낸 시점에 **재지시 총합이 2회 미만**이면,
2차 조건으로 넘어가지 않고 중단한다.

그 경우 결론: **"난이도를 올려도 바닥 효과가 남았다"** 로 기록한다.

---

## v2 변경 이력

| # | 변경 | 이유 |
|---|---|---|
| 1 | 재지시 상한 5회 추가 | 무한 재지시로 한 과업에 시간이 쏠리는 것 방지 |
| 2 | 과업1 기준2를 부정형으로 명확화 | "포함 또는 병합" 표현의 해석 여지 제거 |
| 3 | 과업2 기준3 완화 | 표기 스타일 차이로 인한 가짜 재지시 방지 |
