# 실험 02 프롬프트 원문 (고정)

> 1차·2차 모두 아래 문장을 **그대로** 붙여넣는다.
> 두 조건의 차이는 **2차 앞머리 한 줄뿐**이다.
> 완료 조건은 여기에 적지 않는다 (진행자 전용 `grading-key.md` 에 있다).

---

## [2차 전용] 각 프롬프트 맨 앞에 붙이는 한 줄

```
reflection.md 를 먼저 읽고,
```

---

## 과업 1 — parse-log

```
sample.log 에서 오류(ERROR)만 골라 표로 정리하는 parse.py 를 작성해라.
작성 후 실행해서 결과를 보여줘라.
```

## 과업 2 — refactor-util

```
util.py 의 함수 3개 전부에 타입 힌트와 독스트링을 추가해라.
함수의 동작은 바꾸지 마라.
```

## 과업 3 — fix-failing

```
test_me.py 를 pytest 로 실행하면 2건이 실패한다.
테스트부는 수정하지 말고, 구현부만 고쳐서 4건 전부 통과시켜라.
```

---

## 측정 규칙

- 시작 시각 = 프롬프트를 보낸 순간
- 종료 시각 = `grading-key.md` 기준을 충족했다고 진행자가 판정한 순간
- 재지시 = `grading-key.md` 의 재지시 문구를 보낸 횟수
- 승인 = 권한 승인 창에서 Yes를 누른 횟수
- 모델 처리 시간 = 화면에 표시되는 초 단위 값 합계
